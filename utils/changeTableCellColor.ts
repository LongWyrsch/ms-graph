export const changeTableCellColor = (headersColor: string, evenRowsColor: string, oddRowsColor: string, cellsBorder: string, fontColor: string) => {
	// const headers = document.getElementsByClassName('nutritionHeaders')
	// const evenRows = document.getElementsByClassName('nutritionEvenRows')
	// const oddRows = document.getElementsByClassName('nutritionOddRows')
	// const cells = document.getElementsByClassName('nutritionCells')

    // for (let i = 0; i < headers.length; i++) {
	// 	const element = headers[i] as HTMLElement
	// 	element.style.backgroundColor = headersColor
    //     element.style.fontWeight = '400'
	// }

    // for (let i = 0; i < evenRows.length; i++) {
	// 	const element = evenRows[i] as HTMLElement
	// 	element.style.backgroundColor = evenRowsColor
	// }

    // for (let i = 0; i < oddRows.length; i++) {
	// 	const element = oddRows[i] as HTMLElement
	// 	element.style.backgroundColor = oddRowsColor
	// }

    // for (let i = 0; i < cells.length; i++) {
	// 	const element = cells[i] as HTMLElement
	// 	element.style.border = 'solid ' + cellsBorder
	// 	element.style.borderWidth = '0 1px 1px 0'
    //     element.style.color = fontColor
	// }
}
